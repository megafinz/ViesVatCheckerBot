{
  "name": "viesvatchecker",
  "version": "0.0.0",
  "private": true,
  "description": "Telegram bot for monitoring VAT number availability in VIES.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck",
    "format": "biome format --write apps packages package.json tsconfig.json bunfig.toml biome.json",
    "format:check": "biome format apps packages package.json tsconfig.json bunfig.toml biome.json",
    "lint": "biome lint apps packages package.json tsconfig.json bunfig.toml biome.json",
    "lint:fix": "biome check --write apps packages package.json tsconfig.json bunfig.toml biome.json",
    "lint:staged": "lint-staged",
    "dev:backend": "bun --watch apps/backend/src/index.ts",
    "dev:admin-web": "bun --watch apps/admin-web/src/index.ts",
    "db:migrate": "bun apps/migrator/src/index.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "@types/bun": "1.3.14",
    "lint-staged": "17.0.8",
    "typescript": "5.2.2"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,jsonc,css,html,md}": "biome check --write --no-errors-on-unmatched"
  },
  "packageManager": "bun@1.3.14"
}
